# MAX Chat Bot

🤖 Полнофункциональный чат-бот для мессенджера MAX на Python

## 🚀 Возможности

- ✅ Обработка команд и текстовых сообщений
- ✅ Система сессий и состояний пользователей
- ✅ Ограничение частоты запросов (rate limiting)
- ✅ Административные команды
- ✅ Логирование всех действий
- ✅ Поддержка webhook и polling
- ✅ База данных для хранения данных пользователей
- ✅ Расширяемая архитектура

## 📁 Структура проекта

```
max-chat-bot/
├── main.py              # Главный файл запуска
├── max_chatbot.py       # Основной класс бота
├── config.py            # Конфигурация
├── utils.py             # Утилиты и вспомогательные функции
├── requirements.txt     # Python зависимости
├── .env.example         # Пример переменных окружения
├── README.md           # Документация
└── bot_data.json       # База данных (создается автоматически)
```

## 🛠 Установка

### 1. Клонирование и установка зависимостей

```bash
# Создайте директорию для бота
mkdir max-chat-bot
cd max-chat-bot

# Установите Python зависимости
pip install -r requirements.txt
```

### 2. Настройка конфигурации

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте файл .env
nano .env
```

Установите ваш токен бота MAX:
```env
MAX_BOT_TOKEN=ваш_реальный_токен_здесь
```

### 3. Настройка токена в MAX

1. Откройте настройки мессенджера MAX
2. Перейдите в раздел "Боты" или "Разработка"
3. Создайте нового бота
4. Скопируйте полученный токен
5. Вставьте токен в файл `.env`

## 🚀 Запуск

### Простой запуск
```bash
python main.py
```

### Запуск в фоне (Linux/Mac)
```bash
nohup python main.py &
```

### Запуск с systemd (Linux)
Создайте файл `/etc/systemd/system/max-bot.service`:

```ini
[Unit]
Description=MAX Chat Bot
After=network.target

[Service]
Type=simple
User=your-username
WorkingDirectory=/path/to/max-chat-bot
ExecStart=/usr/bin/python3 main.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable max-bot
sudo systemctl start max-bot
```

## 📱 Команды бота

### Базовые команды
- `/start` - Запустить бота
- `/help` - Показать помощь
- `/time` - Текущее время
- `/echo <текст>` - Повторить текст
- `/info` - Информация о боте
- `/stats` - Статистика пользователя

### Административные команды
- `/admin stats` - Статистика бота
- `/admin users` - Список пользователей
- `/admin broadcast <сообщение>` - Рассылка всем
- `/admin shutdown` - Остановить бота

### Дополнительные команды
- `/calc <выражение>` - Калькулятор
- `/weather <город>` - Погода (требует API ключ)
- `/reminder <время> <текст>` - Напоминание

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `MAX_BOT_TOKEN` | Токен бота MAX | обязательно |
| `WEBHOOK_URL` | URL для webhook | polling режим |
| `LOG_LEVEL` | Уровень логирования | INFO |
| `ADMIN_IDS` | ID администраторов | пусто |

### Настройка конфигурации в коде

Отредактируйте `config.py`:

```python
@dataclass
class BotConfig:
    BOT_TOKEN: str = "ваш_токен"
    API_BASE_URL: str = "https://api.max-messenger.com/bot"
    ADMIN_IDS: list = ["123456789", "987654321"]
```

## 🔧 Разработка

### Добавление новой команды

```python
def my_custom_command(bot_instance, chat_id: str, args: list, message: dict):
    """Моя пользовательская команда"""
    bot_instance.send_message(chat_id, "Привет из новой команды!")

# Регистрация команды
bot.add_command_handler("/mycommand", my_custom_command)
```

### Добавление обработчика сообщений

```python
def my_message_handler(chat_id: str, text: str, message: dict):
    """Обработчик сообщений"""
    if "привет" in text.lower():
        bot.send_message(chat_id, "И тебе привет!")

# Регистрация обработчика
bot.add_message_handler(my_message_handler)
```

### Работа с базой данных

```python
# Сохранение данных пользователя
bot.database.set_user_data(user_id, "score", 100)

# Получение данных пользователя
score = bot.database.get_user_data(user_id).get("score", 0)

# Глобальные данные
bot.database.set_global_data("total_messages", 1000)
```

## 🐛 Отладка

### Логи
Все логи сохраняются в файл `bot.log`. Для просмотра в реальном времени:

```bash
tail -f bot.log
```

### Режим отладки
Установите уровень логирования DEBUG в `.env`:
```env
LOG_LEVEL=DEBUG
```

### Проверка конфигурации
```python
python -c "from config import BotConfig; print(BotConfig.from_env().BOT_TOKEN)"
```

## 🔒 Безопасность

1. **Никогда не коммитьте** файл `.env` в репозиторий
2. **Используйте webhook** вместо polling в продакшене
3. **Установите rate limiting** для предотвращения спама
4. **Ограничьте права** административных команд
5. **Валидируйте** все пользовательские данные

## 📚 API Reference

### Основные методы бота

```python
# Отправка сообщения
bot.send_message(chat_id, "Текст сообщения")

# Отправка фото
bot.send_photo(chat_id, "https://example.com/photo.jpg", "Подпись")

# Получение обновлений
updates = bot.get_updates(offset=0)

# Установка webhook
bot.set_webhook("https://your-domain.com/webhook")
```

### Утилиты

```python
from utils import MessageFormatter, RateLimiter, UserSession

# Форматирование текста
formatter = MessageFormatter()
parts = formatter.format_text(long_text, max_length=4096)

# Ограничение частоты запросов
limiter = RateLimiter(max_requests=10, window_seconds=60)
if limiter.is_allowed(user_id):
    # Выполнить действие
    pass

# Сессии пользователей
sessions = UserSession(ttl_minutes=30)
user_data = sessions.get_session(user_id)
```

## 🚀 Деплой

### Heroku
```bash
# Создание приложения
heroku create max-chat-bot

# Установка переменных
heroku config:set MAX_BOT_TOKEN=ваш_токен

# Деплой
git push heroku main
```

### Docker
```dockerfile
FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .
CMD ["python", "main.py"]
```

### VPS
1. Скопируйте файлы на сервер
2. Установите зависимости
3. Настройте systemd сервис
4. Запустите бота

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch
3. Внесите изменения
4. Добавьте тесты
5. Создайте Pull Request

## 📄 Лицензия

MIT License - подробности в файле LICENSE

## 📞 Поддержка

- 📧 Email: support@example.com
- 💬 Telegram: @your_support_bot
- 🌐 Сайт: https://your-website.com

## 🔄 Changelog

### v1.0.0 (2026-01-21)
- ✅ Первый релиз
- ✅ Базовая функциональность
- ✅ Поддержка команд
- ✅ Система логирования
- ✅ База данных пользователей

---

🚀 **Готов к использованию!** Удачной разработки!
